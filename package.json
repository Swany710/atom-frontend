{
  "name": "atom-frontend",
  "version": "1.0.0",
  "description": "Static front-end served via Express on Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx live-server public --port=3001 --open=/index.html",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "braces": "^3.0.3",
    "micromatch": "^4.0.8"
  }
}
